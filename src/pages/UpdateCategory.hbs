<div class="columns is-vcentered">
    <div class="column is-two-thirds">
        {{> TitleBar title="PlaceMark" subTitle="Edit Placemark"}}
    </div>
    <div class="column">
        {{> MainNavigator }}
    </div>
</div>

<form class="box"  action="/dashboard/updateCategory/{{category._id}}" method="POST">
    <label>Update User Details:</label>
    <div class="field is-horizontal">
        <div class="field-body">
            <div class="field">
                <input class="input" type="text" name="title" value="{{category.title}}">
            </div>
        </div>
    </div>
    <button class="button is-primary">Update Category</button>
</form>